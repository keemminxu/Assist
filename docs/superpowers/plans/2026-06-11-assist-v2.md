# assist v2 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Discord로 소통하는 24시간 개인 비서 — 클라우드 routines(브리핑·채용공고) + 무료 서버의 대화 봇 + Supabase 단일 기억.

**Architecture:** 정기 작업은 Claude Code 클라우드 스케줄 에이전트가 수행해 Discord webhook으로 push하고, 자유 대화는 GCP e2-micro(무료)의 얇은 discord.py 릴레이가 `claude -p` 헤드리스 호출로 처리한다. 양쪽 모두 Supabase 무료 Postgres를 공유 기억으로 사용한다. Oracle A1 확보는 병행 트랙.

**Tech Stack:** Python 3.12, discord.py, httpx, Claude Code CLI(Max 구독 토큰), Supabase(PostgREST), GCP e2-micro, systemd

**규칙:** 스펙은 `docs/superpowers/specs/2026-06-11-assist-v2-design.md`. 커밋은 main에 직접 + push (`git push origin main`). 커밋 메시지에 Claude 출처 표기(Co-Authored-By 등) 금지.

---

## 파일 구조 (최종)

```
bot/
  __init__.py        (빈 파일)
  config.py          환경변수 → Settings
  supa.py            Supabase REST 얇은 비동기 클라이언트
  claude_runner.py   claude -p 서브프로세스 호출 + 결과 파싱 + 에러 분류
  conversation.py    채널별 직렬 대화 (세션 유지, 429 백오프, 모델 다운시프트)
  heartbeat.py       5분 주기 heartbeat upsert 루프
  discord_bot.py     discord 클라이언트 + 메시지 핸들러 + 2000자 청크 전송
  main.py            엔트리포인트 (조립만)
scripts/
  db.py              비서(Claude)가 Bash로 호출하는 DB CLI
agent/
  CLAUDE.md          비서 페르소나 + 도구 사용 지침 (claude -p 의 작업 디렉토리)
routines/
  morning-briefing.md / job-scan.md / evening-checkin.md   루틴 프롬프트 원본
db/
  schema.sql         Supabase 테이블 5개 + RLS
deploy/
  assist-bot.service systemd 유닛
  setup-gcp.md       GCP 인스턴스 생성·셋업 절차
tests/
  test_supa.py / test_claude_runner.py / test_conversation.py
  test_heartbeat.py / test_chunking.py
```

---

### Task 1: v1 정리 + 새 골격

**Files:**
- Delete: `bot.py`, `openclaw.config.json5`, `deploy/discord-bot.service`, `deploy/openclaw.service`
- Create: `bot/__init__.py`, `requirements.txt`(교체), `requirements-dev.txt`, `.env.example`, `pytest.ini`, `.gitignore`

- [ ] **Step 1: v1 파일 삭제** (`oci-retry/`, `my_memos.txt`는 유지)

```bash
git rm bot.py openclaw.config.json5 deploy/discord-bot.service deploy/openclaw.service
```

- [ ] **Step 2: 골격 파일 작성**

`requirements.txt`:
```
discord.py>=2.4
httpx>=0.27
python-dotenv>=1.0
```

`requirements-dev.txt`:
```
pytest>=8
pytest-asyncio>=0.24
```

`pytest.ini`:
```ini
[pytest]
asyncio_mode = auto
```

`.env.example`:
```
DISCORD_TOKEN=
ASSIST_CHANNEL_ID=
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=
CLAUDE_BIN=claude
CLAUDE_MODEL=sonnet
CLAUDE_FALLBACK_MODEL=haiku
AGENT_DIR=agent
```

`.gitignore`:
```
.env
__pycache__/
.venv/
*.pyc
.pytest_cache/
```

`bot/__init__.py`: 빈 파일.

- [ ] **Step 3: 가상환경 + 의존성 설치 확인**

```bash
python -m venv .venv && .venv/Scripts/pip install -r requirements.txt -r requirements-dev.txt
```
Expected: 에러 없이 설치 완료.

- [ ] **Step 4: Commit & push**

```bash
git add -A && git commit -m "v1 정리 및 v2 골격: 의존성·환경 템플릿" && git push origin main
```

---

### Task 2: Discord 서버·웹훅·봇 앱 준비 (사용자 보조 필요)

**Files:** 없음 (외부 셋업). 얻은 값은 로컬 `.env`에만 기록.

- [ ] **Step 1: 채널 3개 생성** — 사용자의 Discord 서버에 `#비서`, `#브리핑`, `#채용` 텍스트 채널 생성 (사용자 작업, 안내만 제공)

- [ ] **Step 2: webhook 2개 생성** — `#브리핑` 채널 설정 → 연동 → 웹후크 → 새 웹후크 → URL 복사. `#채용`도 동일. (사용자 작업)

- [ ] **Step 3: webhook 검증** — 각 URL에 대해:

```bash
curl -X POST -H "Content-Type: application/json" -d '{"content":"webhook 테스트 ✅"}' "<WEBHOOK_URL>"
```
Expected: HTTP 204, Discord 채널에 메시지 표시. **이 두 URL을 `routines/` 등록 시 사용하므로 안전한 곳에 기록** (`.env`에 `WEBHOOK_BRIEFING_URL=`, `WEBHOOK_JOBS_URL=` 추가).

- [ ] **Step 4: 봇 애플리케이션 생성** (사용자 작업, 안내 제공)
  1. https://discord.com/developers/applications → New Application → 이름 `assist`
  2. Bot 탭 → Reset Token → 토큰 복사 → `.env`의 `DISCORD_TOKEN`
  3. 같은 탭에서 **MESSAGE CONTENT INTENT 켜기** (필수)
  4. 초대: `https://discord.com/oauth2/authorize?client_id=<APP_ID>&scope=bot&permissions=68608` (보기+전송+기록읽기)
  5. `#비서` 채널 우클릭 → ID 복사 (개발자 모드 필요) → `.env`의 `ASSIST_CHANNEL_ID`

---

### Task 3: Supabase 스키마 적용 + 기존 메모 이관

**Files:**
- Create: `db/schema.sql`
- Delete: `my_memos.txt` (이관 후)

- [ ] **Step 1: 스키마 작성**

`db/schema.sql`:
```sql
-- assist v2 초기 스키마. 접근은 service role 키로만 (RLS 켜서 anon 차단).

create table if not exists memories (
  id bigint generated always as identity primary key,
  content text not null,
  category text not null default 'fact',      -- fact | preference | context
  source text not null,                        -- bot | routine
  created_at timestamptz not null default now()
);

create table if not exists meals (
  id bigint generated always as identity primary key,
  eaten_at timestamptz not null default now(),
  meal_type text not null default 'snack',     -- breakfast | lunch | dinner | snack
  description text not null,
  note text,
  created_at timestamptz not null default now()
);

create table if not exists job_postings (
  id bigint generated always as identity primary key,
  url_hash text not null unique,
  url text not null,
  title text not null,
  company text,
  site text not null,                          -- wanted | gamejob | saramin | jobkorea
  summary text,
  status text not null default 'new',          -- new | reported | interested | dismissed
  found_at timestamptz not null default now()
);

create table if not exists schedule_notes (
  id bigint generated always as identity primary key,
  note text not null,
  due_at timestamptz,
  done boolean not null default false,
  created_at timestamptz not null default now()
);

create table if not exists heartbeat (
  component text primary key,
  last_seen timestamptz not null default now()
);

alter table memories enable row level security;
alter table meals enable row level security;
alter table job_postings enable row level security;
alter table schedule_notes enable row level security;
alter table heartbeat enable row level security;
```

- [ ] **Step 2: Supabase MCP로 적용** — `mcp__claude_ai_Supabase__list_projects`로 프로젝트 확인(없으면 무료 프로젝트 생성), `apply_migration`(name: `assist_v2_init`)으로 schema.sql 적용

- [ ] **Step 3: 검증** — `list_tables` 결과에 5개 테이블 존재 확인

- [ ] **Step 4: my_memos.txt 이관** — `execute_sql`:

```sql
insert into schedule_notes (note) values
  ('이번 주말에 몽구 데리고 하남스타필드 다녀오기'),
  ('이번 주말에 몽구 데리고 고양 스타필드 다녀오기');
```
이후 `git rm my_memos.txt`.

- [ ] **Step 5: Commit & push**

```bash
git add -A && git commit -m "Supabase 스키마 5테이블 + 기존 메모 이관" && git push origin main
```

---

### Task 4: 채용 사이트 수집 스파이크 (최대 리스크 검증)

**Files:**
- Create: `docs/superpowers/research/2026-06-11-job-sites.md`

- [ ] **Step 1: 4개 사이트 직접 fetch 가능성 확인** — WebFetch(또는 curl)로 각각 시도하고 결과 기록:

| 사이트 | 시도 URL |
|---|---|
| 원티드 | `https://www.wanted.co.kr/api/v4/jobs?query=언리얼&country=kr&job_sort=job.latest_order&limit=20` (공개 JSON API 후보) |
| 게임잡 | `https://www.gamejob.co.kr/List_GI/GIB_Search.asp?Keyword=언리얼` |
| 사람인 | `https://www.saramin.co.kr/zf_user/search?searchword=언리얼` |
| 잡코리아 | `https://www.jobkorea.co.kr/Search/?stext=언리얼` |

기록 항목: HTTP 상태, 차단 여부(봇 감지 페이지), 공고 목록 파싱 가능 여부, 실제 공고 제목 3개 추출 시도 결과.

- [ ] **Step 2: 막힌 사이트는 검색 경유 fallback 검증** — WebSearch로 `site:saramin.co.kr 언리얼 채용` 형태가 신규 공고를 잡아내는지 확인. 그것도 부실하면 해당 사이트는 "수집 불가, 명시 보고" 처리로 확정.

- [ ] **Step 3: 결과 문서화** — `docs/superpowers/research/2026-06-11-job-sites.md`에 사이트별 판정(직접 fetch / 검색 경유 / 불가)과 사용할 URL·파싱 방법을 기록. **이 판정이 Task 14의 job-scan 프롬프트에 그대로 들어간다.**

- [ ] **Step 4: Commit & push**

```bash
git add docs/superpowers/research && git commit -m "채용 사이트 4곳 수집 가능성 스파이크 결과" && git push origin main
```

---

### Task 5: morning-briefing 루틴 등록 (클라우드 커넥터 검증 게이트)

**Files:**
- Create: `routines/morning-briefing.md`

- [ ] **Step 1: 루틴 프롬프트 작성**

`routines/morning-briefing.md`:
```markdown
# morning-briefing (매일 07:30 Asia/Seoul)

너는 김민수의 개인 비서다. 아침 브리핑을 만들어 Discord로 보내라.

1. Gmail 커넥터(search_threads)로 지난 24시간 수신 메일을 조회해 요약한다.
   - 중요한 메일(개인·업무·면접·결제)은 한 건씩, 광고·뉴스레터는 묶어서 한 줄로.
2. Google Calendar 커넥터(list_events)로 오늘 일정을 가져온다.
3. 아래 형식으로 2000자 이내로 정리해 webhook으로 전송한다 (초과 시 나눠서 여러 번 POST):

   curl -X POST -H "Content-Type: application/json" \
     -d '{"content":"<브리핑 내용>"}' "<WEBHOOK_BRIEFING_URL>"

4. 어떤 단계든 실패하면 그 사실과 사유를 같은 webhook으로 보고한다. 침묵 금지.

## 출력 형식
## ☀️ 아침 브리핑 — {YYYY-MM-DD (요일)}
**📧 메일 ({중요 건수}건 중요 / {전체}건)**
- ...
**📅 오늘 일정**
- HH:MM 일정명
(일정 없으면 "오늘 일정 없음")
```

- [ ] **Step 2: `/schedule`로 등록** — 위 프롬프트에서 `<WEBHOOK_BRIEFING_URL>`을 실제 URL로 치환해 매일 07:30 Asia/Seoul 크론으로 등록.

- [ ] **Step 3: 즉시 1회 실행으로 검증** — `/schedule`의 일회 실행으로 지금 돌려서 #브리핑 채널에 브리핑이 도착하는지 확인. **핵심 확인사항: 클라우드 실행 환경에서 Gmail/Calendar 커넥터가 실제로 동작하는가.**
  - 커넥터가 안 붙으면: 이 기능을 Task 13 이후 서버 측 cron(`claude -p` + gcalcli/IMAP)으로 옮기는 결정을 내리고 스펙에 기록. (게이트 — 여기서 막히면 사용자와 상의)

- [ ] **Step 4: Commit & push**

```bash
git add routines && git commit -m "morning-briefing 루틴 프롬프트 + 클라우드 등록" && git push origin main
```

---

### Task 6: bot/supa.py — Supabase REST 클라이언트

**Files:**
- Create: `bot/supa.py`
- Test: `tests/test_supa.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_supa.py`:
```python
import httpx
from bot.supa import Supa


def make_supa(handler):
    return Supa("https://x.supabase.co", "secret-key",
                transport=httpx.MockTransport(handler))


async def test_upsert_targets_table_with_merge_duplicates():
    captured = {}

    def handler(request):
        captured["url"] = str(request.url)
        captured["prefer"] = request.headers.get("Prefer")
        captured["apikey"] = request.headers.get("apikey")
        return httpx.Response(201, json=[])

    supa = make_supa(handler)
    await supa.upsert("heartbeat",
                      {"component": "bot", "last_seen": "2026-06-11T00:00:00Z"},
                      on_conflict="component")
    assert "/rest/v1/heartbeat" in captured["url"]
    assert "on_conflict=component" in captured["url"]
    assert captured["prefer"] == "resolution=merge-duplicates"
    assert captured["apikey"] == "secret-key"


async def test_upsert_raises_on_http_error():
    def handler(request):
        return httpx.Response(500, json={"message": "boom"})

    supa = make_supa(handler)
    try:
        await supa.upsert("heartbeat", {"component": "bot"}, on_conflict="component")
        assert False, "예외가 나야 함"
    except httpx.HTTPStatusError:
        pass
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/Scripts/python -m pytest tests/test_supa.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'bot.supa'`

- [ ] **Step 3: 구현**

`bot/supa.py`:
```python
"""Supabase PostgREST 얇은 비동기 클라이언트 (service role 키 사용)."""
from __future__ import annotations

import httpx


class Supa:
    def __init__(self, url: str, service_key: str, transport=None):
        self._client = httpx.AsyncClient(
            base_url=f"{url}/rest/v1",
            headers={
                "apikey": service_key,
                "Authorization": f"Bearer {service_key}",
            },
            timeout=15.0,
            transport=transport,
        )

    async def upsert(self, table: str, row: dict, *, on_conflict: str) -> None:
        resp = await self._client.post(
            f"/{table}",
            params={"on_conflict": on_conflict},
            json=row,
            headers={"Prefer": "resolution=merge-duplicates"},
        )
        resp.raise_for_status()

    async def aclose(self) -> None:
        await self._client.aclose()
```

- [ ] **Step 4: 통과 확인**

Run: `.venv/Scripts/python -m pytest tests/test_supa.py -v`
Expected: 2 passed

- [ ] **Step 5: Commit & push**

```bash
git add bot/supa.py tests/test_supa.py && git commit -m "Supabase REST upsert 클라이언트" && git push origin main
```

---

### Task 7: bot/claude_runner.py — claude -p 호출·파싱

**Files:**
- Create: `bot/claude_runner.py`
- Test: `tests/test_claude_runner.py`

- [ ] **Step 1: 실패하는 테스트 작성** (파싱 로직은 순수 함수로 분리해 테스트)

`tests/test_claude_runner.py`:
```python
import json

import pytest

from bot.claude_runner import ClaudeError, RateLimited, _parse_result


def test_parse_success_returns_reply_and_session():
    out = json.dumps({"type": "result", "subtype": "success",
                      "result": "  안녕하세요!  ", "session_id": "sess-abc"})
    reply, sid = _parse_result(0, out, "")
    assert reply == "안녕하세요!"
    assert sid == "sess-abc"


def test_parse_nonzero_with_429_raises_rate_limited():
    with pytest.raises(RateLimited):
        _parse_result(1, "", "API Error: 429 rate limit exceeded")


def test_parse_nonzero_with_limit_reached_raises_rate_limited():
    with pytest.raises(RateLimited):
        _parse_result(1, "5-hour limit reached", "")


def test_parse_nonzero_other_raises_claude_error():
    with pytest.raises(ClaudeError):
        _parse_result(1, "", "segfault")


def test_parse_zero_but_invalid_json_raises_claude_error():
    with pytest.raises(ClaudeError):
        _parse_result(0, "이건 JSON 아님", "")
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/Scripts/python -m pytest tests/test_claude_runner.py -v`
Expected: FAIL — import 에러

- [ ] **Step 3: 구현**

`bot/claude_runner.py`:
```python
"""claude -p 헤드리스 호출. Max 구독 토큰(CLAUDE_CODE_OAUTH_TOKEN env)으로 인증."""
from __future__ import annotations

import asyncio
import json


class ClaudeError(Exception):
    """claude CLI 호출 실패 (일반)."""


class RateLimited(ClaudeError):
    """구독 한도(429/limit) 도달."""


_RATE_MARKERS = ("429", "rate limit", "limit reached")


def _parse_result(returncode: int, stdout: str, stderr: str) -> tuple[str, str]:
    if returncode != 0:
        combined = f"{stdout}\n{stderr}".lower()
        if any(m in combined for m in _RATE_MARKERS):
            raise RateLimited(combined.strip()[:300])
        raise ClaudeError(combined.strip()[:300])
    try:
        data = json.loads(stdout)
    except json.JSONDecodeError as e:
        raise ClaudeError(f"JSON 파싱 실패: {stdout[:200]}") from e
    if data.get("subtype") != "success":
        raise ClaudeError(f"비정상 종료: {data.get('subtype')}")
    return (data.get("result") or "").strip(), data["session_id"]


async def run_claude(
    prompt: str,
    *,
    session_id: str | None = None,
    model: str = "sonnet",
    claude_bin: str = "claude",
    cwd: str = "agent",
    timeout: float = 300.0,
) -> tuple[str, str]:
    """프롬프트를 보내고 (응답, session_id)를 반환. 세션 지정 시 이어서 대화."""
    cmd = [claude_bin, "-p", prompt,
           "--output-format", "json",
           "--model", model,
           "--dangerously-skip-permissions"]
    if session_id:
        cmd += ["--resume", session_id]
    proc = await asyncio.create_subprocess_exec(
        *cmd, cwd=cwd,
        stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE,
    )
    try:
        out, err = await asyncio.wait_for(proc.communicate(), timeout)
    except asyncio.TimeoutError:
        proc.kill()
        raise ClaudeError(f"{timeout:.0f}초 안에 응답 없음")
    return _parse_result(proc.returncode or 0,
                         out.decode("utf-8", "replace"),
                         err.decode("utf-8", "replace"))
```

참고: `--dangerously-skip-permissions`는 전용 서버의 전용 계정(`assist`)에서만 돌리는 전제. Windows 로컬 테스트에서 `claude`가 안 잡히면 `.env`의 `CLAUDE_BIN`에 `claude.exe` 전체 경로를 지정.

- [ ] **Step 4: 통과 확인**

Run: `.venv/Scripts/python -m pytest tests/test_claude_runner.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit & push**

```bash
git add bot/claude_runner.py tests/test_claude_runner.py && git commit -m "claude -p 헤드리스 러너 + 429 분류" && git push origin main
```

---

### Task 8: bot/conversation.py — 직렬 큐·백오프·다운시프트

**Files:**
- Create: `bot/conversation.py`
- Test: `tests/test_conversation.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_conversation.py`:
```python
from bot.claude_runner import ClaudeError, RateLimited
from bot.conversation import Conversation


async def test_keeps_session_between_asks():
    calls = []

    async def fake_runner(prompt, *, session_id=None, model=None):
        calls.append((prompt, session_id, model))
        return f"re:{prompt}", "sess-1"

    c = Conversation(runner=fake_runner)
    assert await c.ask("안녕") == "re:안녕"
    assert await c.ask("뭐해") == "re:뭐해"
    assert calls[0][1] is None
    assert calls[1][1] == "sess-1"      # 두 번째부터 세션 이어받기


async def test_rate_limit_backs_off_then_downshifts():
    sleeps, attempts = [], []

    async def fake_sleep(s):
        sleeps.append(s)

    async def fake_runner(prompt, *, session_id=None, model=None):
        attempts.append(model)
        if len(attempts) < 3:
            raise RateLimited("429")
        return "ok", "s"

    c = Conversation(runner=fake_runner, model="sonnet",
                     fallback_model="haiku", sleep=fake_sleep)
    assert await c.ask("hi") == "ok"
    assert sleeps == [30, 90]
    assert attempts == ["sonnet", "sonnet", "haiku"]


async def test_exhausted_rate_limit_returns_friendly_message():
    async def fake_runner(prompt, *, session_id=None, model=None):
        raise RateLimited("429")

    async def fake_sleep(s):
        pass

    c = Conversation(runner=fake_runner, sleep=fake_sleep)
    reply = await c.ask("hi")
    assert "한도" in reply


async def test_claude_error_returns_error_message_without_retry():
    count = 0

    async def fake_runner(prompt, *, session_id=None, model=None):
        nonlocal count
        count += 1
        raise ClaudeError("boom")

    c = Conversation(runner=fake_runner)
    reply = await c.ask("hi")
    assert "오류" in reply
    assert count == 1
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/Scripts/python -m pytest tests/test_conversation.py -v`
Expected: FAIL — import 에러

- [ ] **Step 3: 구현**

`bot/conversation.py`:
```python
"""채널 하나의 직렬 대화: 세션 유지 + 429 백오프 + 모델 다운시프트."""
from __future__ import annotations

import asyncio
import logging

from bot.claude_runner import ClaudeError, RateLimited

log = logging.getLogger(__name__)

_BACKOFFS = (0, 30, 90, 180)        # 시도 4회: 즉시, 30s, 90s, 180s
_DOWNSHIFT_FROM = 2                 # 3번째 시도(인덱스 2)부터 폴백 모델

LIMIT_MESSAGE = "지금 Claude 사용 한도에 걸려 있어요. 잠시 뒤에 다시 말 걸어주세요. 🙏"


class Conversation:
    def __init__(self, runner, *, model: str = "sonnet",
                 fallback_model: str = "haiku", sleep=asyncio.sleep):
        self._runner = runner
        self._model = model
        self._fallback = fallback_model
        self._sleep = sleep
        self._session_id: str | None = None
        self._lock = asyncio.Lock()     # 채널 내 직렬 처리

    async def ask(self, prompt: str) -> str:
        async with self._lock:
            for i, delay in enumerate(_BACKOFFS):
                if delay:
                    await self._sleep(delay)
                model = self._fallback if i >= _DOWNSHIFT_FROM else self._model
                try:
                    reply, sid = await self._runner(
                        prompt, session_id=self._session_id, model=model)
                    self._session_id = sid
                    return reply
                except RateLimited:
                    log.warning("rate limited (시도 %d/%d)", i + 1, len(_BACKOFFS))
                except ClaudeError as e:
                    log.error("claude 오류: %s", e)
                    return f"비서 두뇌 호출 중 오류가 났어요: {e}"
            return LIMIT_MESSAGE
```

- [ ] **Step 4: 통과 확인**

Run: `.venv/Scripts/python -m pytest tests/test_conversation.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit & push**

```bash
git add bot/conversation.py tests/test_conversation.py && git commit -m "대화 직렬화 + 429 백오프 + 모델 다운시프트" && git push origin main
```

---

### Task 9: bot/heartbeat.py — 생존 신호 루프

**Files:**
- Create: `bot/heartbeat.py`
- Test: `tests/test_heartbeat.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_heartbeat.py`:
```python
import asyncio

import pytest

from bot.heartbeat import heartbeat_loop


async def test_upserts_then_sleeps_each_cycle():
    rows = []

    class FakeSupa:
        async def upsert(self, table, row, *, on_conflict):
            rows.append((table, row["component"], on_conflict))

    ticks = 0

    async def fake_sleep(seconds):
        nonlocal ticks
        assert seconds == 300
        ticks += 1
        if ticks >= 3:
            raise asyncio.CancelledError

    with pytest.raises(asyncio.CancelledError):
        await heartbeat_loop(FakeSupa(), sleep=fake_sleep)
    assert rows == [("heartbeat", "discord-bot", "component")] * 3


async def test_upsert_failure_does_not_kill_loop():
    calls = 0

    class FlakySupa:
        async def upsert(self, table, row, *, on_conflict):
            nonlocal calls
            calls += 1
            raise RuntimeError("db down")

    ticks = 0

    async def fake_sleep(seconds):
        nonlocal ticks
        ticks += 1
        if ticks >= 2:
            raise asyncio.CancelledError

    with pytest.raises(asyncio.CancelledError):
        await heartbeat_loop(FlakySupa(), sleep=fake_sleep)
    assert calls == 2       # 실패해도 다음 주기 계속
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/Scripts/python -m pytest tests/test_heartbeat.py -v`
Expected: FAIL — import 에러

- [ ] **Step 3: 구현**

`bot/heartbeat.py`:
```python
"""5분마다 heartbeat 테이블 갱신. evening-checkin 루틴이 30분 무신호를 사망으로 판정."""
from __future__ import annotations

import asyncio
import logging
from datetime import datetime, timezone

log = logging.getLogger(__name__)

INTERVAL_SECONDS = 300


async def heartbeat_loop(supa, *, component: str = "discord-bot",
                         sleep=asyncio.sleep) -> None:
    while True:
        try:
            await supa.upsert(
                "heartbeat",
                {"component": component,
                 "last_seen": datetime.now(timezone.utc).isoformat()},
                on_conflict="component",
            )
        except Exception as e:           # noqa: BLE001 — 신호 실패가 봇을 죽이면 안 됨
            log.warning("heartbeat 실패: %s", e)
        await sleep(INTERVAL_SECONDS)
```

- [ ] **Step 4: 통과 확인**

Run: `.venv/Scripts/python -m pytest tests/test_heartbeat.py -v`
Expected: 2 passed

- [ ] **Step 5: Commit & push**

```bash
git add bot/heartbeat.py tests/test_heartbeat.py && git commit -m "5분 주기 heartbeat 루프" && git push origin main
```

---

### Task 10: bot/discord_bot.py + config.py + main.py — 조립

**Files:**
- Create: `bot/discord_bot.py`, `bot/config.py`, `bot/main.py`
- Test: `tests/test_chunking.py`

- [ ] **Step 1: 청크 분할 실패 테스트 작성**

`tests/test_chunking.py`:
```python
from bot.discord_bot import chunk_message


def test_short_text_is_single_chunk():
    assert chunk_message("안녕") == ["안녕"]


def test_long_text_splits_at_2000():
    chunks = chunk_message("a" * 4500)
    assert [len(c) for c in chunks] == [2000, 2000, 500]


def test_empty_text_becomes_placeholder():
    assert chunk_message("") == ["(빈 응답)"]
    assert chunk_message("   ") == ["(빈 응답)"]
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/Scripts/python -m pytest tests/test_chunking.py -v`
Expected: FAIL — import 에러

- [ ] **Step 3: 구현 3개 파일**

`bot/config.py`:
```python
"""환경변수 → Settings. .env는 로컬/서버 공통."""
from __future__ import annotations

import os
from dataclasses import dataclass

from dotenv import load_dotenv


@dataclass(frozen=True)
class Settings:
    discord_token: str
    assist_channel_id: int
    supabase_url: str
    supabase_service_key: str
    claude_bin: str
    claude_model: str
    claude_fallback_model: str
    agent_dir: str

    @classmethod
    def from_env(cls) -> "Settings":
        load_dotenv()
        return cls(
            discord_token=os.environ["DISCORD_TOKEN"],
            assist_channel_id=int(os.environ["ASSIST_CHANNEL_ID"]),
            supabase_url=os.environ["SUPABASE_URL"],
            supabase_service_key=os.environ["SUPABASE_SERVICE_KEY"],
            claude_bin=os.getenv("CLAUDE_BIN", "claude"),
            claude_model=os.getenv("CLAUDE_MODEL", "sonnet"),
            claude_fallback_model=os.getenv("CLAUDE_FALLBACK_MODEL", "haiku"),
            agent_dir=os.getenv("AGENT_DIR", "agent"),
        )
```

`bot/discord_bot.py`:
```python
"""Discord 게이트웨이 클라이언트 — #비서 채널 메시지를 Conversation에 직렬 전달."""
from __future__ import annotations

import logging

import discord

log = logging.getLogger(__name__)

DISCORD_MAX_MSG = 2000


def chunk_message(text: str, limit: int = DISCORD_MAX_MSG) -> list[str]:
    if not text.strip():
        return ["(빈 응답)"]
    return [text[i:i + limit] for i in range(0, len(text), limit)]


def create_client(settings, make_conversation) -> discord.Client:
    intents = discord.Intents.default()
    intents.message_content = True
    client = discord.Client(intents=intents)
    conversations: dict[int, object] = {}

    @client.event
    async def on_ready():
        log.info("비서 봇 가동 | user=%s | channel=%s",
                 client.user, settings.assist_channel_id)

    @client.event
    async def on_message(message: discord.Message):
        if (message.author.bot
                or message.channel.id != settings.assist_channel_id
                or not message.content):
            return
        conv = conversations.setdefault(message.channel.id, make_conversation())
        try:
            async with message.channel.typing():
                reply = await conv.ask(message.content)
        except Exception as e:           # noqa: BLE001 — 마지막 방어선
            log.exception("메시지 처리 실패")
            reply = f"처리 중 예상 못 한 오류: {e}"
        for chunk in chunk_message(reply):
            await message.channel.send(chunk)

    return client
```

`bot/main.py`:
```python
"""엔트리포인트 — 조립만 한다."""
from __future__ import annotations

import asyncio
import logging
from functools import partial

from bot.claude_runner import run_claude
from bot.config import Settings
from bot.conversation import Conversation
from bot.discord_bot import create_client
from bot.heartbeat import heartbeat_loop
from bot.supa import Supa


async def amain() -> None:
    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    )
    s = Settings.from_env()
    supa = Supa(s.supabase_url, s.supabase_service_key)
    runner = partial(run_claude, claude_bin=s.claude_bin, cwd=s.agent_dir)
    client = create_client(
        s,
        lambda: Conversation(runner=runner, model=s.claude_model,
                             fallback_model=s.claude_fallback_model),
    )
    asyncio.ensure_future(heartbeat_loop(supa))
    await client.start(s.discord_token)


def main() -> None:
    try:
        asyncio.run(amain())
    except KeyboardInterrupt:
        pass


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 전체 테스트 통과 확인**

Run: `.venv/Scripts/python -m pytest -v`
Expected: 전부 passed (supa 2 + runner 5 + conversation 4 + heartbeat 2 + chunking 3 = 16)

- [ ] **Step 5: Commit & push**

```bash
git add bot tests && git commit -m "discord 클라이언트·설정·엔트리포인트 조립" && git push origin main
```

---

### Task 11: scripts/db.py + agent/CLAUDE.md — 비서의 손과 인격

**Files:**
- Create: `scripts/db.py`, `agent/CLAUDE.md`

- [ ] **Step 1: DB CLI 작성**

`scripts/db.py`:
```python
#!/usr/bin/env python3
"""비서(Claude)가 Bash로 호출하는 Supabase CLI.

사용 예:
  python scripts/db.py remember "민수는 매운 음식을 좋아함" --category preference
  python scripts/db.py recall 음식
  python scripts/db.py meal "제육볶음" --type lunch
  python scripts/db.py meals
  python scripts/db.py note "금요일 치과" --due 2026-06-13T10:00:00+09:00
  python scripts/db.py notes
  python scripts/db.py note-done 3
"""
from __future__ import annotations

import argparse
import json
import os

import httpx
from dotenv import load_dotenv


def _client() -> httpx.Client:
    load_dotenv()
    url = os.environ["SUPABASE_URL"]
    key = os.environ["SUPABASE_SERVICE_KEY"]
    return httpx.Client(
        base_url=f"{url}/rest/v1",
        headers={"apikey": key, "Authorization": f"Bearer {key}",
                 "Prefer": "return=representation"},
        timeout=15.0,
    )


def _print(resp: httpx.Response) -> None:
    resp.raise_for_status()
    print(json.dumps(resp.json(), ensure_ascii=False, indent=2, default=str))


def main() -> None:
    p = argparse.ArgumentParser(description=__doc__)
    sub = p.add_subparsers(dest="cmd", required=True)

    r = sub.add_parser("remember", help="사실/선호 저장")
    r.add_argument("content")
    r.add_argument("--category", default="fact",
                   choices=["fact", "preference", "context"])

    rc = sub.add_parser("recall", help="기억 키워드 검색")
    rc.add_argument("keyword")

    m = sub.add_parser("meal", help="식단 기록")
    m.add_argument("description")
    m.add_argument("--type", default="snack",
                   choices=["breakfast", "lunch", "dinner", "snack"])
    m.add_argument("--note")

    sub.add_parser("meals", help="최근 식단 10건")

    n = sub.add_parser("note", help="일정 메모 추가")
    n.add_argument("note")
    n.add_argument("--due", help="ISO8601 예: 2026-06-13T10:00:00+09:00")

    sub.add_parser("notes", help="미완료 일정 메모 목록")

    nd = sub.add_parser("note-done", help="일정 메모 완료 처리")
    nd.add_argument("note_id", type=int)

    a = p.parse_args()
    c = _client()
    if a.cmd == "remember":
        _print(c.post("/memories", json={"content": a.content,
                                         "category": a.category, "source": "bot"}))
    elif a.cmd == "recall":
        _print(c.get("/memories", params={"content": f"ilike.*{a.keyword}*",
                                          "order": "created_at.desc", "limit": "20"}))
    elif a.cmd == "meal":
        body = {"description": a.description, "meal_type": a.type}
        if a.note:
            body["note"] = a.note
        _print(c.post("/meals", json=body))
    elif a.cmd == "meals":
        _print(c.get("/meals", params={"order": "eaten_at.desc", "limit": "10"}))
    elif a.cmd == "note":
        body = {"note": a.note}
        if a.due:
            body["due_at"] = a.due
        _print(c.post("/schedule_notes", json=body))
    elif a.cmd == "notes":
        _print(c.get("/schedule_notes", params={"done": "eq.false",
                                                "order": "created_at.asc"}))
    elif a.cmd == "note-done":
        _print(c.patch("/schedule_notes", params={"id": f"eq.{a.note_id}"},
                       json={"done": True}))


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 수동 검증** (Supabase에 실제 기록)

```bash
.venv/Scripts/python scripts/db.py remember "테스트 기억" --category context
.venv/Scripts/python scripts/db.py recall 테스트
```
Expected: 첫 명령은 생성된 row JSON, 둘째는 검색 결과에 "테스트 기억" 포함.

- [ ] **Step 3: 비서 페르소나 작성**

`agent/CLAUDE.md`:
```markdown
# 비서 작업 공간

너는 김민수의 개인 비서다. Discord #비서 채널의 메시지가 그대로 프롬프트로 들어온다.

## 사용자
- 언리얼 엔진 개발자. 한국어 사용.
- 핵심 관심사: 일정 관리, 식단 기록, 언리얼 채용 시장.

## 도구 — 기억과 기록은 전부 scripts/db.py로 (Bash)
- 사실·선호 저장: `python ../scripts/db.py remember "<내용>" --category preference|fact|context`
- 기억 검색:      `python ../scripts/db.py recall <키워드>`
- 식단 기록:      `python ../scripts/db.py meal "<메뉴>" --type breakfast|lunch|dinner|snack`
- 최근 식단:      `python ../scripts/db.py meals`
- 일정 메모:      `python ../scripts/db.py note "<내용>" --due <ISO8601>`
- 미완료 메모:    `python ../scripts/db.py notes` / 완료: `note-done <id>`

## 행동 규칙
1. 사용자가 먹은 것을 말하면(예: "점심에 제육 먹었어") 조용히 meal로 기록하고 짧게 확인해줘라.
2. 사용자에 대해 새로 알게 된 지속적 사실(선호, 습관, 사람, 목표)은 remember로 저장해라.
3. 일정 질문은 notes 조회 + 필요 시 웹검색으로 답해라. 캘린더 원본은 아침 브리핑(#브리핑 채널)이 다룬다.
4. 답변은 Discord 마크다운으로 간결하게. 핵심 먼저. 2000자 안에 끝내는 걸 기본으로.
5. 모르는 건 아는 척하지 말고 모른다고 해라. 최신 정보는 웹검색을 써라.
```

- [ ] **Step 4: 헤드리스 연동 스모크 테스트**

```bash
claude -p "방금 점심으로 김치찌개 먹었어" --output-format json --model haiku --dangerously-skip-permissions
```
(작업 디렉토리 `agent/`에서 실행)
Expected: JSON `subtype: success`, 이후 `python scripts/db.py meals` 결과에 김치찌개 row 존재.

- [ ] **Step 5: Commit & push**

```bash
git add scripts agent && git commit -m "비서 DB CLI + 페르소나 워크스페이스" && git push origin main
```

---

### Task 12: 로컬 통합 테스트 (Windows)

**Files:** 없음 (실행 검증)

- [ ] **Step 1: .env 완성 확인** — `DISCORD_TOKEN`, `ASSIST_CHANNEL_ID`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` 채워졌는지. Windows에서 `claude`가 PATH에 없으면 `CLAUDE_BIN`에 전체 경로.

- [ ] **Step 2: 봇 기동**

```bash
.venv/Scripts/python -m bot.main
```
Expected: 로그에 `비서 봇 가동 | user=assist#...`

- [ ] **Step 3: 실대화 검증** — Discord #비서 채널에서:
  1. "안녕, 내 이름 기억해?" → 응답 오는지
  2. "저녁에 삼겹살 먹었어" → 응답 + `scripts/db.py meals`에 기록 확인
  3. 긴 답변 유도("언리얼 5.5 새 기능 자세히 알려줘") → 2000자 분할 전송 확인
  4. Supabase `heartbeat` 테이블에 `discord-bot` row의 `last_seen` 갱신 확인

- [ ] **Step 4: 문제 있으면 superpowers:systematic-debugging으로 원인 규명 후 수정·커밋**

---

### Task 13: GCP e2-micro 배포

**Files:**
- Create: `deploy/assist-bot.service`, `deploy/setup-gcp.md`

- [ ] **Step 1: systemd 유닛 작성**

`deploy/assist-bot.service`:
```ini
[Unit]
Description=assist discord personal assistant bot
After=network-online.target
Wants=network-online.target

[Service]
User=assist
WorkingDirectory=/opt/assist
EnvironmentFile=/opt/assist/.env
ExecStart=/opt/assist/.venv/bin/python -m bot.main
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 2: 셋업 절차 문서 작성**

`deploy/setup-gcp.md`:
```markdown
# GCP e2-micro 셋업 (Always Free)

무료 조건: e2-micro, us-west1/us-central1/us-east1, 표준 영구디스크 30GB 이하.

## 1. 인스턴스 생성 (로컬 gcloud 또는 콘솔)
gcloud compute instances create assist-bot \
  --zone=us-west1-b --machine-type=e2-micro \
  --image-family=ubuntu-2404-lts-amd64 --image-project=ubuntu-os-cloud \
  --boot-disk-size=30GB --boot-disk-type=pd-standard

## 2. 기본 셋업 (ssh 접속 후)
sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile \
  && sudo mkswap /swapfile && sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
sudo apt update && sudo apt install -y python3-venv git curl
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash - \
  && sudo apt install -y nodejs
sudo npm install -g @anthropic-ai/claude-code
sudo useradd -m -s /bin/bash assist

## 3. 코드 배치
sudo git clone https://github.com/keemminxu/Assist.git /opt/assist
sudo chown -R assist:assist /opt/assist
sudo -u assist python3 -m venv /opt/assist/.venv
sudo -u assist /opt/assist/.venv/bin/pip install -r /opt/assist/requirements.txt

## 4. 인증
sudo -u assist claude setup-token   # 브라우저 URL 안내 따라 진행
# 발급된 토큰을 /opt/assist/.env 에 CLAUDE_CODE_OAUTH_TOKEN=... 으로 추가
# 로컬 .env의 나머지 값(DISCORD_TOKEN 등)도 /opt/assist/.env 로 복사
sudo chmod 600 /opt/assist/.env && sudo chown assist:assist /opt/assist/.env

## 5. 서비스 등록
sudo cp /opt/assist/deploy/assist-bot.service /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now assist-bot
systemctl status assist-bot     # active (running) 확인
journalctl -u assist-bot -f     # "비서 봇 가동" 로그 확인
```

- [ ] **Step 3: 실제 배포 수행** — 위 문서대로 진행 (인스턴스 생성·과금 확인은 사용자 결정 필요 시 확인 요청)

- [ ] **Step 4: 원격 검증** — 로컬 봇 종료 후 Discord에서 대화 → 서버 봇이 응답하는지, e2-micro에서 응답 시간 체감(목표: 첫 토큰 1분 이내; 과도하게 느리면 $4 VPS 승급 결정)

- [ ] **Step 5: Commit & push**

```bash
git add deploy && git commit -m "GCP e2-micro 배포 유닛·절차" && git push origin main
```

---

### Task 14: job-scan + evening-checkin 루틴 등록

**Files:**
- Create: `routines/job-scan.md`, `routines/evening-checkin.md`

- [ ] **Step 1: job-scan 프롬프트 작성** (Task 4 스파이크 판정으로 사이트별 수집 방법 부분을 채움)

`routines/job-scan.md`:
```markdown
# job-scan (매일 09:00 Asia/Seoul)

너는 김민수(언리얼 엔진 개발자)의 채용 정찰 비서다. 새 언리얼 공고만 골라 보고하라.

## 1. 수집 — 키워드 "언리얼" / "Unreal"
(사이트별 방법은 스파이크 판정 결과를 그대로 기입:
 직접 fetch 가능 사이트는 URL, 검색 경유 사이트는 WebSearch 쿼리,
 불가 판정 사이트는 "수집 불가"로 명시하고 보고에도 그렇게 적는다)
- 원티드: <스파이크 판정 결과 기입>
- 게임잡: <스파이크 판정 결과 기입>
- 사람인: <스파이크 판정 결과 기입>
- 잡코리아: <스파이크 판정 결과 기입>

## 2. 중복 제거 — Supabase 커넥터 execute_sql로 공고마다:
insert into job_postings (url_hash, url, title, company, site, summary)
values (md5('<URL>'), '<URL>', '<제목>', '<회사>', '<사이트>', '<한 줄 요약>')
on conflict (url_hash) do nothing
returning id;
→ id가 반환된 것만 "신규"다. 신규만 보고한다.

## 3. 보고 — webhook으로 POST (2000자 초과 시 분할):
curl -X POST -H "Content-Type: application/json" \
  -d '{"content":"<보고 내용>"}' "<WEBHOOK_JOBS_URL>"

## 출력 형식
## 🎮 언리얼 채용 — {YYYY-MM-DD}
**신규 {N}건**
- **{회사}** {제목} — {한 줄 요약} <{URL}>
(신규 없으면 "오늘 신규 공고 없음 ({수집 성공 사이트 수}/4 사이트 확인)")
수집 실패한 사이트가 있으면 마지막 줄에 명시한다.

## 실패 시
어떤 단계든 실패하면 실패 사유를 같은 webhook으로 보고. 침묵 금지.
```

- [ ] **Step 2: evening-checkin 프롬프트 작성**

`routines/evening-checkin.md`:
```markdown
# evening-checkin (매일 21:00 Asia/Seoul)

너는 김민수의 개인 비서다. 하루를 닫는 체크인을 만들어 Discord로 보내라.

1. Google Calendar 커넥터(list_events)로 **내일** 일정을 가져온다.
2. Supabase 커넥터 execute_sql로 오늘(KST) 식단을 조회한다:
   select meal_type, description, to_char(eaten_at at time zone 'Asia/Seoul', 'HH24:MI') as t
   from meals
   where eaten_at >= (date_trunc('day', now() at time zone 'Asia/Seoul')
                      at time zone 'Asia/Seoul')
   order by eaten_at;
   - 기록이 0건이면 "오늘 식단 기록이 없어요. #비서 채널에 먹은 것 알려주세요"를 포함.
3. 봇 생존 확인 — execute_sql:
   select component, last_seen,
          now() - last_seen > interval '30 minutes' as dead
   from heartbeat where component = 'discord-bot';
   - dead=true 또는 row 없음이면 보고에 "⚠️ 대화 봇이 응답하지 않는 상태"를 포함.
4. webhook으로 POST (2000자 초과 시 분할):
   curl -X POST -H "Content-Type: application/json" \
     -d '{"content":"<체크인 내용>"}' "<WEBHOOK_BRIEFING_URL>"
5. 실패 시 실패 사유를 같은 webhook으로 보고. 침묵 금지.

## 출력 형식
## 🌙 저녁 체크인 — {YYYY-MM-DD}
**📅 내일 일정** ...
**🍽 오늘 식단** ...
**🤖 봇 상태** 정상 / ⚠️ 응답 없음
```

- [ ] **Step 3: `/schedule`로 2개 등록** — webhook URL 치환 후 각각 09:00, 21:00 Asia/Seoul 등록. 각각 일회 실행으로 즉시 검증 (#채용, #브리핑 채널 수신 확인).

- [ ] **Step 4: Commit & push**

```bash
git add routines && git commit -m "job-scan·evening-checkin 루틴 프롬프트 + 등록" && git push origin main
```

---

### Task 15: README + 운영 런북

**Files:**
- Create: `README.md`

- [ ] **Step 1: README 작성** — 다음 내용 포함:
  - 한 단락 소개 + 아키텍처 다이어그램(스펙의 ASCII 재사용)
  - 구성 요소별 위치: 루틴(`routines/`, /schedule로 등록), 봇(`bot/`), DB(`db/schema.sql`), 배포(`deploy/`)
  - 운영 런북: 봇 재시작(`sudo systemctl restart assist-bot`), 로그(`journalctl -u assist-bot -f`), 루틴 수정 절차(파일 수정 → /schedule 재등록), 새 테이블 추가 절차
  - 스펙·계획 문서 링크

- [ ] **Step 2: Commit & push**

```bash
git add README.md && git commit -m "README: 아키텍처·운영 런북" && git push origin main
```

---

### Task 16: Oracle A1 병행 트랙 (백그라운드, 사용자 보조)

**Files:**
- Modify: `deploy/setup-gcp.md` → 말미에 Oracle 이전 절차 추가

- [ ] **Step 1: PAYG 전환 안내** (사용자 작업) — OCI 콘솔 → Billing → Upgrade to Pay-As-You-Go. 직후 **Budget 생성: 월 $1 초과 시 이메일 알림** (Billing → Budgets).

- [ ] **Step 2: capacity 확보 시도** — Oracle CloudShell에서 기존 `oci-retry/retry-launch.sh` 실행 (OCID 기입 완료 상태). PAYG 전환 후엔 보통 수 분~수 시간 내 성공.

- [ ] **Step 3: 이전 절차 문서화** — `deploy/setup-gcp.md` 말미에 추가:

```markdown
## Oracle A1 이전 (capacity 확보 시)
1. A1 인스턴스에 setup-gcp.md 2~5단계 동일 적용 (ARM이지만 절차 동일)
2. GCP의 /opt/assist/.env 를 그대로 복사
3. A1에서 systemctl enable --now assist-bot → Discord 대화 검증
4. GCP 쪽 sudo systemctl disable --now assist-bot
5. 24시간 병행 관찰 후 GCP 인스턴스 삭제:
   gcloud compute instances delete assist-bot --zone=us-west1-b
```

- [ ] **Step 4: Commit & push**

```bash
git add deploy/setup-gcp.md && git commit -m "Oracle A1 이전 절차" && git push origin main
```

---

## 자체 점검 결과 (스펙 대비)

- 스펙 §4.1 루틴 3종 → Task 5, 14 / §4.2 봇 → Task 6~12 / §4.3 DB → Task 3 / §4.4 Oracle → Task 16 / §5 채널 → Task 2 / §6 에러 처리 → 루틴 프롬프트의 침묵 금지 + Conversation 백오프 + heartbeat 사망 판정 / §7 검증 순서 → Task 2(웹훅) → 4(채용 스파이크) → 5(커넥터 게이트) → 12~13(봇) 순서 유지 / §8 비범위 준수.
- 캘린더 **쓰기**(일정 등록)는 v2 범위에서 읽기·메모(schedule_notes) 중심으로 시작 — 클라우드 루틴이 읽기를 담당하고, 봇의 캘린더 직접 조작은 운용해보고 필요하면 후속(gcalcli 등) 결정.
- 미지 요소 게이트 2곳: Task 4(사이트 차단), Task 5(클라우드 커넥터). 둘 다 막히면 대안이 명시돼 있음.
